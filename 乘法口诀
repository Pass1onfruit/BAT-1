#include <stdio.h>

int main()
{
	int a, b;

	for (a=1; a<=9; a++)
	{
		for (b=1; b<=a; b++)
		{
			if (b<a)
				printf("%d * %d = %d  ", a, b, a*b);
			else if (a=b)
				printf("%d * %d = %d\n", a, b, a*b);
		}
	}
	return 0;
}
